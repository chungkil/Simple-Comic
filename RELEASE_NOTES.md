# Simple Comic 1.8.0 Release Notes

1.7.2 이후 30개 커밋. 핵심은 웹툰 모드, 라이브러리 윈도우, 페이지 편집/리오더, 작품별 진행률 저장입니다.

---

## 신기능

### 웹툰 모드 (⌘J)

세로로 길게 이어붙이는 한국식 웹툰을 위한 연속 스크롤 모드.

- 모든 페이지를 세로 1열로 무한 스크롤
- 폭이 다른 페이지는 가장 넓은 페이지 기준 가운데 정렬 (비율 유지)
- 키보드(↑↓ / Space / PgUp · PgDn) 스크롤 및 ⌥-스크롤 줌 / 루뻬 지원
- 보류 중인 회전을 실시간 미리보기
- 페이지 간격 / 최대 폭 설정 (보기 메뉴)

### 라이브러리 윈도우 (⌘Y)

지정 폴더를 스캔해 표지 그리드로 작품을 한 눈에.

- 표지 썸네일 그리드 + 슬라이더로 크기 조절
- 여러 폴더 등록 + 재귀 스캔 (이미지 폴더는 leaf 작품으로 인식)
- 검색 / 정렬 / 미확인(unread) 뱃지
- 파일이 없어진 작품은 흐리게 표시
- "이어 읽기" 셸프: 최근 본 작품 가로 스크롤

### 작품별 진행률 & 북마크

- 마지막 페이지 + 스크롤 위치 + 레이아웃 모드를 작품별로 영구 저장 (앱 종료해도 복원)
- 이름 있는 북마크 추가 / 수정 / 개별 삭제
- 북마크 메뉴 항목에 썸네일 미리보기
- 다중 소스 세션은 진행률 키 무효화 (잘못된 작품에 저장 방지)

### 페이지 편집 (CBZ 인플레이스 수정)

- **삭제**: Remove Page 후 창 닫을 때 일괄 적용, CBZ 재패킹, AppleDouble 고스트(`._*`) 제거
- **회전**: 페이지 단위 좌/우 회전 + 모든 페이지 일괄 회전 (저장 시 실제 이미지에 반영)
- **순서 변경**: ⌘[ / ⌘] 또는 썸네일 익스포제(T)에서 드래그-리오더, 창 닫을 때 아카이브 재구성
- **CBR / 7z → CBZ 변환**: 비-zip 아카이브를 편집 가능한 CBZ 형제 파일로 변환
- 재정렬 시 하위 폴더 구조 보존 옵션

### 썸네일 익스포제 재작성 (T)

NSCollectionView 기반으로 완전히 새로 작성. 기존 구현이 모던 macOS에서 검은 화면만 나오는 문제 해결.

- HUD 블러 배경에 표지 격자
- 셀 호버 → 720pt 큰 미리보기 팝업
- 클릭 → 점프 & 닫힘
- Esc → 닫힘
- 드래그 → 재정렬 (위 페이지 편집과 동일 경로)

### 독서 통계 윈도우

누적 독서 작품 수 / 완독 비율 등 요약.

---

## 개선

- **페이지 프리페치**: 페이지 넘김 시 디코드 블로킹 제거 (앞·뒤 페이지 미리 디코드)
- **루뻬**:
  - 기본 직경 500 → 1200pt
  - ⌘+스크롤 변경값을 즉시 저장 → 다음 실행 시 복원
  - 최대 직경 클램프 500 → 1500pt
- **버전 자동화**: git 태그(`git describe`)로부터 빌드 번호 자동 생성 (`xcconfig` 경유)

---

## 기타

- Redacted Safari 확장 통합 (별도 타깃, 메인 앱과 무관)

---

## 호환성

페이지 편집(삭제 / 회전 / 리오더)은 **CBZ 또는 폴더에서만** 직접 작동. CBR / 7z는 "Convert to CBZ" 후 편집 가능.
